---
date: 2026-03-31
run_id: 2026-03-31T16-49-14-design-doc
project: Skaldmark
spec_source: docs/factory/2026-03-31T16-49-14-design-doc/spec.md
dimensions:
  spec_fidelity: Green
  agent_decision_quality: Green
  phase_efficiency: Red
  evidence_completeness: Yellow
  error_recovery: Yellow
critical_count: 1
important_count: 1
suggestion_count: 1
status: resolved
---

# Factory Run Retro: 2026-03-31T16-49-14-design-doc

**Run Date:** 2026-03-31
**Duration:** ~5min CLI + manual fallback (in progress)
**Input Type:** design-doc (bug fixes from test run — 3 bugs)
**Spec Score:** 0/3 (pre-fix baseline)

## Dimension Scores

| Dimension | Score |
|---|---|
| Spec Fidelity | Green |
| Agent Decision Quality | Green |
| Phase Efficiency | Red |
| Evidence Completeness | Yellow |
| Error Recovery | Yellow |

## Critical Findings

### 1. "Agent returned no result" at preflight — 6th consecutive run

**Root Cause:** infrastructure
**Pattern:** recurring (seen in: Skaldmark fix #3, LTP fix #1, LTP batch #1, LTP T02-27-14, Skaldmark T11-56-48 fix #2)
**Destination:** factory config change

Preflight returned null after 1m32s — the 6th consecutive factory run with this error across 2 projects. The null_result retry + canary health check + model escalation fixes were implemented in the logic-tool-plugins repo (forge v2.11.1) but the Skaldmark project was running the OLD CLI version without those fixes.

**Impact:** Pipeline aborted; manual fallback required.
**Proposed Fix:** Deploy forge-factory v2.11.1 to Skaldmark's CLI by updating the plugin cache or reinstalling. The fixes exist — they just aren't deployed to this project yet. This is a deployment gap, not a code gap.

---

## Important Findings

### 2. Test-driven bug fix input produces the best factory specs

**Root Cause:** new learning
**Pattern:** new
**Destination:** new learning

This run started from concrete test failures (49 scenarios, 3 FAIL, 2 PARTIAL) with exact problem descriptions, verified file paths, and testable acceptance criteria. Result: forge produced a clean spec on first attempt, 3 sub-specs mapped 1:1 to 3 bugs, zero path hallucination, zero phantom sub-specs, correct decomposition.

Compare to the prior Skaldmark run (T11-56-48) which started from a design doc: 5/6 preflight CRITs were wrong paths, 1 phantom sub-spec, 12 sub-specs over-decomposed. Test-failure inputs bypass all three of those failure modes because the input already contains verified data.

**Impact:** Positive finding — no rework needed.
**Proposed Fix:** Encourage the pattern: run `/forge-test-create` + `/forge-test-run` first, then feed failures to `/forge-dark-factory`. The test run validates file paths and APIs, the factory spec inherits that validation. Add this as a recommended workflow in the forge docs/CLAUDE.md.

---

## Suggestions

### 3. Small bug-fix runs (3 sub-specs) are the factory's sweet spot

**Root Cause:** new learning
**Pattern:** new
**Destination:** new learning

Forge (1m48s) + prep (1m50s) = 3m38s thinking pipeline. 3 independent sub-specs with clear inputs. Workers SS-02 and SS-03 completed quickly. The only blocker was the preflight null-result failure (addressed by v2.11.1 fixes).

**Proposed Fix:** No code change needed. Document that the factory's optimal input size is 2-5 focused sub-specs with test-validated file paths. Larger runs (10+ sub-specs) should be split into multiple factory runs rather than one monolithic pass.
